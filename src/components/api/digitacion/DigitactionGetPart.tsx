import { FileText } from "lucide-react"

const DigitactionGetPart = () => {
  return (
    <div className="w-full flex items-start justify-center">
        <button className="text-xs text-gray-600 pb-2 cursor-pointer hover:underline transform hover:scale-105 hover:text-gray-700 transition-all duration-300"> 
            <FileText />
        </button>
    </div>
  )
}

export default DigitactionGetPart